<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.leafgarden">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SensorPlanta"></activity>
        <activity android:name=".MeuJardim" />
        <activity android:name=".AdcPlanta" />
        <activity android:name=".InfoPlanta" />
        <activity android:name=".Plantas" />
        <activity android:name=".TelaMenu" />
        <activity android:name=".TelaCadastroLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>